import React from "react";

function Heading() {
  const info ="The filter() method takes a callback parameter, and returns an array containing all values that the callback returned true for. That makes ieasy to use for filtering an array of objects. For example, given a listof characters, heres how you can find all characters whose teamproperty is Avengers.";
  const websitName = "WE ARE GOING BIGGER LTD"
  return (
    <div>
      {/* Website name */}
      <h1></h1>
      {/* info about website */}
      <p></p>
    </div>
  );
}

export default Heading;